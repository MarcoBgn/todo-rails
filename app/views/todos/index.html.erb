<%= link_to "Add a new Todo", new_todo_path %>


      <ul class='todos'>
        <% @todos.each do |todo| %>
        <li class="<%= 'completed' if todo.completed? %>"> <%= todo.title %>
             <%= check_box(todo, :Completed, {id: "Check_todo"}) %>
             <%= button_to "Mark Completed", todo_completion_path(todo) %>
        </li>
        <% end %>
      </ul>
